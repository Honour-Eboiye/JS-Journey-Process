/**
  let y = 100;
  let x = 30;

  const sum = y + x;
  console.log(sum);

  const sub = y - x;
  console.log(sub);

  const mult = y * x;
  console.log(mult);

  const division = y / x;
  console.log(division);

  const modulus = y % x;
  console.log(modulus);

  const exponential = y**x;
  console.log(exponential); 
 */

  let calculation = `1 + 1`;
  console.log(calculation);
